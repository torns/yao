spring:
  application:
    name: auth-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: AUTH_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

