jasypt:
  encryptor:
    password: 123456

spring:
  application:
    name: y3tu-cloud-authorization
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: y3tu-cloud-config
        enabled: true
      profile: ${spring.profiles.active}
      label: master
      username: y3tu
      password: yxy727

---
spring:
  profiles: dev
eureka:
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 5
    lease-expiration-duration-in-seconds: 20
  client:
    serviceUrl:
      defaultZone: http://y3tu:yxy727@${EUREKA_ZONE_HOST:localhost}:${EUREKA_ZONE_PORT:8501}/eureka
    registry-fetch-interval-seconds: 10

---
spring:
  profiles: prd
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://pig:gip6666@eureka:1025/eureka

