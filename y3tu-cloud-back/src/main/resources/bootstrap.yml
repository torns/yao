spring:
  application:
    name: y3tu-cloud-back-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: BACK_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true