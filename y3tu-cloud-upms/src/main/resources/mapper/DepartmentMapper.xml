<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.y3tu.cloud.upms.mapper.DepartmentMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.y3tu.cloud.upms.model.entity.Department" id="departmentMap">
            <result property="id" column="id"/>
            <result property="createBy" column="create_by"/>
            <result property="createTime" column="create_time"/>
            <result property="delFlag" column="del_flag"/>
            <result property="updateBy" column="update_by"/>
            <result property="updateTime" column="update_time"/>
            <result property="parentId" column="parent_id"/>
            <result property="sortOrder" column="sort_order"/>
            <result property="status" column="status"/>
            <result property="name" column="name"/>
            <result property="isParent" column="is_parent"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id as id,
        create_by as createBy,
        create_time as createTime,
        del_flag as delFlag,
        update_by as updateBy,
        update_time as updateTime,
        parent_id as parentId,
        sort_order as sortOrder,
        status as status,
        name as name,
        is_parent as isParent,
    1=1
    </sql>


    <select id="queryPage" resultType="com.y3tu.cloud.upms.model.entity.Department">
        select
        <include refid="Base_Column_List"/>
        from t_department
    </select>
</mapper>
