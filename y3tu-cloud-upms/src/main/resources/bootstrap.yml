spring:
  application:
    name: upms-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: UPMS_GROUP
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true